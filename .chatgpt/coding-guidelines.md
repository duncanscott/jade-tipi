# ChatGPT Coding Guidelines

- Use `snake_case` for JSON property names instead of `camelCase` unless the user explicitly requests otherwise.
- Keep JSON examples and fixtures consistent with this naming so downstream clients can rely on a uniform format.
