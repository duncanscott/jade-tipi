rootProject.name = 'jade-tipi'

dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files("backend/libraries/jade-tipi-id/gradle/libs.versions.toml"))
        }
    }
}

include 'backend:jade-tipi'
include 'backend:libraries:jade-tipi-id'