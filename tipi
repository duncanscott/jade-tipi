#!/bin/sh

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
CLI_PATH="$SCRIPT_DIR/clients/tipi/build/install/tipi/bin/tipi"

if [ ! -x "$CLI_PATH" ]; then
  echo "Tipi CLI not installed. Run: ./gradlew :clients:tipi:installDist" >&2
  exit 1
fi

exec "$CLI_PATH" "$@"
