{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://jade-tipi.org/schema/unit.schema.json",
  "title": "Unit",
  "description": "A unit of measurement with optional SI prefix and imported quantity metadata. ID format: <system>~<unit> or <system>~<unit>~<prefix>",
  "type": "object",
  "required": ["unit", "symbol", "system"],
  "additionalProperties": false,
  "properties": {
    "unit": {
      "type": "string",
      "minLength": 1,
      "description": "Unit name",
      "examples": ["volt", "metre", "second"]
    },
    "prefix": {
      "type": ["string", "null"],
      "description": "SI prefix, or null for the base unit",
      "examples": ["kilo", "milli", "micro", null]
    },
    "symbol": {
      "type": "string",
      "minLength": 1,
      "description": "Unit symbol including prefix symbol",
      "examples": ["V", "kV", "mV"]
    },
    "property": {
      "type": ["string", "null"],
      "description": "Physical quantity measured by the unit",
      "examples": ["electric potential difference", "length", null]
    },
    "alternate_unit": {
      "type": ["array", "null"],
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "Alternate names imported with the unit",
      "examples": [["metre"], ["metric ton"], null]
    },
    "system": {
      "type": "string",
      "minLength": 1,
      "description": "Unit system",
      "examples": ["SI"]
    },
    "id": {
      "type": "string",
      "description": "Composite ID: <system>~<unit> or <system>~<unit>~<prefix>",
      "readOnly": true
    }
  },
  "examples": [
    {
      "unit": "volt",
      "prefix": null,
      "symbol": "V",
      "property": "electric potential difference",
      "system": "SI"
    },
    {
      "unit": "volt",
      "prefix": "kilo",
      "symbol": "kV",
      "property": "electric potential difference",
      "system": "SI"
    }
  ]
}
